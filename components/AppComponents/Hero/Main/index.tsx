import React from 'react'
import HeroLayout from './styles'

interface HeroProps {
    
}

const Hero = ({}: HeroProps) => {
    return (
        <HeroLayout></HeroLayout>
    )
}

export default Hero
