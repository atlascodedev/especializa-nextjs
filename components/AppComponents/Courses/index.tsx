import React from "react";
import styled from "styled-components";

interface Props {}

const Courses = (props: Props) => {
  return <div>this is the courses section</div>;
};

export default Courses;
